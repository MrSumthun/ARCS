# Minimal runtime requirements for ARCS
# Install with: pip3 install -r requirements.txt

PyQt6>=6.6
Pillow
reportlab

# Optional dev/test dependencies:
# pytest  # install separately if you add tests
